[server]
    port=2333

[datasource "default"]
    username=wpm
    password=wpm
    database=wpm
    driver=sqlxxx
    port=0
    host=localhost
    lazyload=true

[datasource "wpm"]
    username=wpm
    password=wpm
    database=wpm
    driver=sqlite
    port=0
    host=localhost
